apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: {{ .Values.abstractName }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
  - name: http
    port: {{ .Values.service.port }}